import { useState } from "react";
const fields = [["pregnancies","Số lần mang thai"],["glucose","Glucose"],["blood_pressure","Huyết áp"],["skin_thickness","Độ dày da"],["insulin","Insulin"],["bmi","BMI"],["diabetes_pedigree_function","Chỉ số di truyền"],["age","Tuổi"]] as const;
type Form = Record<typeof fields[number]["0"], number>;
export default function App(){const [form,setForm]=useState<Form>({pregnancies:1,glucose:120,blood_pressure:70,skin_thickness:20,insulin:80,bmi:30,diabetes_pedigree_function:.4,age:30});const [result,setResult]=useState<any>(null);const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setError("");try{const r=await fetch("http://127.0.0.1:8001/predict/diabetes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});if(!r.ok)throw Error();setResult(await r.json())}catch{setError("Không thể kết nối API. Hãy khởi động FastAPI trên cổng 8001.")}}return <main><header className="top"><div><div className="brand">PULSEBOARD</div><div className="eyebrow">DIABETES PREDICTION / 01</div></div><div className="eyebrow mint">● MÔ HÌNH ĐANG HOẠT ĐỘNG</div></header><section className="intro"><div className="eyebrow mint">DIABETES DASHBOARD</div><h1>Dự đoán bệnh tiểu đường</h1><p>Nhập các chỉ số y tế để ước tính nguy cơ mắc bệnh.</p></section><div className="grid"><section className="panel"><div className="eyebrow">01 / DỮ LIỆU ĐẦU VÀO</div><h2>Thông tin bệnh nhân</h2><form onSubmit={submit}>{fields.map(([key,label])=><div className="field" key={key}><label>{label}</label><input type="number" min="0" value={form[key]} onChange={e=>setForm({...form,[key]:Number(e.target.value)||0})}/></div>)}<button className="button">◎ Dự đoán ↗</button>{error&&<div className="error">{error}</div>}</form></section><section className="panel"><div className="eyebrow">02 / KẾT QUẢ MÔ HÌNH</div><h2>Nguy cơ tiểu đường</h2><div className="score">{result?Math.round(result.disease_probability*100):0}%</div><div className="result"><div className="item"><small>XÁC ĐỊNH</small><strong>{result?(result.prediction?"Có nguy cơ":"Nguy cơ thấp"):"Chưa có dữ liệu"}</strong></div><div className="item"><small>MỨC ĐỘ RỦI RO</small><strong>{result?.risk_level??"Chưa có dữ liệu"}</strong></div><div className="item"><small>MÔ HÌNH</small><strong>Logistic Regression</strong></div></div><div className="bar"><div className="fill" style={{width:`${result?result.disease_probability*100:0}%`}}/></div><p className="note">Kết quả hỗ trợ sàng lọc, không thay thế chẩn đoán y khoa.</p></section></div></main>}
